# QuizGenerator
Generate a quiz based on prewritten questions and answers using the ChatGPT API to create similar sounding fake answers.
To make the ChatGPT API call work, edit a .env file with the following format:
OPENAI_API_KEY={your_key}
This can be found at the following url: https://platform.openai.com/account/api-keys